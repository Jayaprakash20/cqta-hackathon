# Copy to .env and fill in your key. The app reads this via python-dotenv.
# The constant name is API_KEY — keep it in sync with config.API_KEY_ENV.
# Without a valid key, DataGuard still runs: the orchestrator falls back to a
# deterministic verdict so the demo never breaks.
API_KEY=
